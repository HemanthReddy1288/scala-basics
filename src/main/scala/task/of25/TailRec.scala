package task.of25

//Write a function tailRec that recursively computes the sum of all elements
// in a list using head, tail, and init. Use recursion instead of higher-order functions
// like foldLeft or reduce.
//
//    Input: A list of integers.
//    Output: The sum of all the elements in the list.
object TailRec extends App {

  //  def tailRec(list : List[Int]) : Int = {
  //
  //  }
  //  def checkerFoldLeft (list2 : List[Int]) : Int ={
  //    list2.foldLeft(acc : Int)((acc,x)=> acc  x))
  //  }
  //  }
  //}
}