id = rs.get(t.id),
  partyKey = rs.get(t.partyKey),
  transactionDate = rs.get(t.transactionDate),
  productCode = rs.get(t.productCode),
  accountCurrency = rs.get(t.accountCurrency),
  amount = rs.get(t.amount),
  transactionCurrency = rs.get(t.transactionCurrency),
  transactionCurrencyAmount = rs.get(t.transactionCurrencyAmount),
  localCurrencyEquivalent = rs.get(t.localCurrencyEquivalent),
  isCredit = rs.get(t.isCredit),
  accountId = rs.get(t.accountId),
  transactionTypeCode = rs.get(t.transactionTypeCode),
  transactionTypeDescription = rs.get(t.transactionTypeDescription),
  beneficiary = rs.get(t.beneficiary),
  originatorAddress = rs.get(t.originatorAddress),
  originatorBankCountryCode = rs.get(t.originatorBankCountryCode),
  beneficiaryAddress = rs.get(t.beneficiaryAddress),
  beneficiaryBankCountryCode = rs.get(t.beneficiaryBankCountryCode),
  remittanceCountry = rs.get(t.remittanceCountry),
  remittancePaymentDetails = rs.get(t.remittancePaymentDetails),
  chequeNo = rs.get(t.chequeNo),
  tellerId = rs.get(t.tellerId),
  yourReference = rs.get(t.yourReference),
  ourReference = rs.get(t.ourReference),
  bankInfo = rs.get(t.bankInfo),
  countryOfTransaction = rs.get(t.countryOfTransaction),
  mcc = rs.get(t.mcc),
  merchantName = rs.get(t.merchantName),
  originator = rs.get(t.originator),
  atmId = rs.get(t.atmId),
  atmLocation = rs.get(t.atmLocation),
  counterParty = rs.get(t.counterParty),
  swiftMsgType = rs.get(t.swiftMsgType),
  swiftMsgInfo = rs.get(t.swiftMsgInfo),
  tdNumber = rs.get(t.tdNumber),
  trancheNo = rs.get(t.trancheNo),
  creditCardNumber = rs.get(t.creditCardNumber),
  highRiskCountryFlag =rs.get(t.highRiskCountryFlag) ,
  destinationRiskFlag= rs.get(t.destinationRiskFlag),
  sourceRiskFlag = rs.get(t.sourceRiskFlag),
  destinationOfFunds = rs.get(t.destinationOfFunds),
  sourceOfFunds = rs.get(t.sourceOfFunds),
  instrument = rs.get(t.instrument),
  beneficiaryType = rs.get(t.beneficiaryType),
  c2cLinkageFlag = rs.get(t.c2cLinkageFlag),
  beneficiaryAdverseFlag = rs.get(t.beneficiaryAdverseFlag),
  beneficialBankName = rs.get(t.beneficiaryBankName),
  originatorBankName = rs.get(t.originatorBankName),
  oppAccountNumber = rs.get(t.oppAccountNumber),
  bankBranchCode = rs.get(t.bankBranchCode),
  transactionPurpose = rs.get(t.transactionPurpose),
  partyCountryRiskLevel  = rs.get(t.partyCountryRiskLevel),
  oppCountryRiskLevel = rs.get(t.oppCountryRiskLevel),
  counterPartyNationality = rs.get(t.counterPartyNationality),
  counterPartyType = rs.get(t.counterPartyType),
  refundToThirdPartyFlag = rs.get(t.refundToThirdPartyFlag),
  ipRiskiness = rs.get(t.ipRiskiness),
  sourceOfTxnFunds = rs.get(t.sourceOfTxnFunds),
  oppCitizenshipCountry = rs.get(t.oppCitizenshipCountry),
  counterpartyType = rs.get(t.counterpartyType),
  )